mutation UnfollowUser($where: UserWhereUniqueInput!) {
	unfollowUser(where: $where) {
		record {
			id
			followers(first: 0) {
				totalCount
			}
			trophies {
				id
				totalFollowers
			}
			viewerFollowing
		}
		viewer {
			id
			following(first: 0) {
				totalCount
			}
			name
		}
	}
}
