mutation UpvotePost($where: PostWhereUniqueInput!) {
	upvotePost(where: $where) {
		record {
			id
			upvotes
			upvoters {
				edges {
					cursor
					node {
						id
					}
				}
				nodes {
					id
				}
			}
		}
	}
}
