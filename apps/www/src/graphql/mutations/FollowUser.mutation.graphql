mutation FollowUser($where: UserWhereUniqueInput!) {
	followUser(where: $where) {
		record {
			id
			followers(first: 0) {
				totalCount
			}
			trophies {
				id
				totalFollowers
			}
			viewerFollowing
		}
		viewer {
			id
			name
		}
	}
}
