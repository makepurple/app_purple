mutation FollowUser($where: UserWhereUniqueInput!) {
	followUser(where: $where) {
		record {
			following {
				... on User {
					id
					viewerFollowing
				}
			}
		}
		viewer {
			id
			name
		}
	}
}
