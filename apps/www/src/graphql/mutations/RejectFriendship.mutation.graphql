mutation RejectFriendship($where: UserWhereUniqueInput!) {
	rejectFriendship(where: $where) {
		record {
			id
			viewerCanFriend
			viewerFriended
			viewerIsFriend
		}
		viewer {
			id
			name
		}
	}
}
