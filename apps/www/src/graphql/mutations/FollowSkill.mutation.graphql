mutation FollowSkill($where: SkillWhereUniqueInput!) {
	followSkill(where: $where) {
		record {
			id
			followers(first: 0) {
				totalCount
			}
			github {
				id
				skill {
					id
				}
			}
			viewerFollowing
		}
		viewer {
			id
			name
		}
	}
}
