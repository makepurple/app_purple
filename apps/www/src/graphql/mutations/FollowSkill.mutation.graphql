mutation FollowSkill($where: SkillWhereUniqueInput!) {
	followSkill(where: $where) {
		record {
			following {
				... on Skill {
					id
					github {
						id
						skill {
							id
						}
					}
					viewerFollowing
				}
			}
		}
		viewer {
			id
			name
		}
	}
}
