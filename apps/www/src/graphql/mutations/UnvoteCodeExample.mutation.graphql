mutation UnvoteCodeExample($where: CodeExampleWhereUniqueInput!) {
	unvoteCodeExample(where: $where) {
		record {
			id
			upvotes
			upvoters(first: 6) {
				edges {
					cursor
					node {
						id
					}
				}
				nodes {
					id
				}
			}
			viewerUpvote
		}
	}
}
