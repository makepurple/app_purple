mutation UnfollowSkill($where: SkillWhereUniqueInput!) {
	unfollowSkill(where: $where) {
		record {
			following {
				... on Skill {
					id
					github {
						id
						skill {
							id
						}
					}
					viewerFollowing
				}
			}
		}
		viewer {
			id
			name
		}
	}
}
