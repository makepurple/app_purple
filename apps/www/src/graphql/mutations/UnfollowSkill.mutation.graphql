mutation UnfollowSkill($where: SkillWhereUniqueInput!) {
	unfollowSkill(where: $where) {
		record {
			id
			followers(first: 0) {
				totalCount
			}
			github {
				id
				skill {
					id
				}
			}
			viewerFollowing
		}
		viewer {
			id
			name
		}
	}
}
