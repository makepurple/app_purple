query SuggestSkills($where: SuggestSkillsWhereInput!) {
	suggestSkills(first: 6, where: $where) {
		__typename
		nodes {
			__typename
			id
			description
			name
			owner {
				__typename
				id
				login
			}
		}
		totalCount
	}
}
