query GetPostDraft {
	postDraft {
		__typename
		id
		content
		description
		skills(first: 8) {
			totalCount
			edges {
				cursor
				node {
					id
				}
			}
			nodes {
				id
				name
				owner
			}
		}
		title
		thumbnailUrl
	}
}
