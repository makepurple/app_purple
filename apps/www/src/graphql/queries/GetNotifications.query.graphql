query GetNotifications($after: String, $first: Int) {
	viewer {
		id
		notifications(
			after: $after
			first: $first
		) {
			pageInfo {
				...PageInfoFragment
			}
			edges {
				cursor
				node {
					id
				}
			}
			nodes {
				...NotificationCardNotification
			}
		}
	}
}
