query GetPost($where: PostWhereUniqueInput!) {
	post(where: $where) {
		__typename
		id
		author {
			id
			name
			image
		}
		authorName
		content
		description
		publishedAt
		title
		urlSlug
		thumbnailUrl
	}
}
