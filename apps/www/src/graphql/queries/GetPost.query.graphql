query GetPost($where: PostWhereUniqueInput!) {
	post(where: $where) {
		__typename
		id
		author {
			id
			name
			image
		}
		authorName
		content
		description
		publishedAt
		skills(first: 50) {
			pageInfo {
				...PageInfoFragment
			}
			edges {
				cursor
				node {
					id
				}
			}
			nodes {
				id
				name
				owner
			}
		}
		title
		upvotes
		urlSlug
		thumbnailUrl
		viewerUpvote
	}
}
