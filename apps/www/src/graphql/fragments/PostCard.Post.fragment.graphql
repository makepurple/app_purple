fragment PostCardPost on Post {
	id
	author {
		id
		name
	}
	authorName
	description
	publishedAt
	skills(first: 6) {
		totalCount
		edges {
			cursor
			node {
				id
			}
		}
		nodes {
			...SkillCardSkill
		}
	}
	thumbnailUrl
	title
	upvotes
	urlSlug
	viewerUpvote
}
