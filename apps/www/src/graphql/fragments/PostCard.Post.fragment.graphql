fragment PostCardPost on Post {
	id
	author {
		id
		name
	}
	authorName
	description
	publishedAt
	readTime
	skills(first: 8) {
		totalCount
		edges {
			cursor
			node {
				id
			}
		}
		nodes {
			...SkillCardSkill
		}
	}
	thumbnailUrl
	title
	upvotes
	urlSlug
	viewerUpvote
}
