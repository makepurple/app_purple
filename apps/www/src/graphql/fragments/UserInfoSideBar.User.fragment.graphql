fragment UserInfoSideBarUser on User {
	desiredSkills(first: 100) {
		pageInfo {
			...PageInfoFragment
		}
		edges {
			cursor
			node {
				id
			}
		}
		nodes {
			id
			name
			owner
		}
	}
	followersCount
	followingCount
	friendsCount
	github {
		id
		bio
		company
		name
		topLanguages {
			...TopLanguages
		}
		twitterUsername
		url
		websiteUrl
	}
	id
	name
	skills(first: 100) {
		pageInfo {
			...PageInfoFragment
		}
		edges {
			cursor
			node {
				id
			}
		}
		nodes {
			id
			name
			owner
		}
	}
	viewerCanFriend
	viewerFollowing
	viewerIsFriend
	...UserAvatarUser
}
