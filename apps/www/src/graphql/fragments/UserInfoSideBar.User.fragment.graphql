fragment UserInfoSideBarUser on User {
	__typename
	desiredSkills {
		__typename
		pageInfo {
			...PageInfoFragment
		}
		edges {
			cursor
			node {
				__typename
				id
			}
		}
		nodes {
			__typename
			id
			name
		}
	}
	followers {
		totalCount
	}
	following {
		totalCount
	}
	github {
		__typename
		id
		bio
		company
		name
		topLanguages {
			__typename
			...TopLanguages
		}
		twitterUsername
		url
		websiteUrl
	}
	id
	name
	skills {
		__typename
		pageInfo {
			...PageInfoFragment
		}
		edges {
			cursor
			node {
				__typename
				id
			}
		}
		nodes {
			__typename
			id
			name
		}
	}
	viewerCanFriend
	viewerFollowing
	viewerIsFriend
	...UserAvatarUser
}
