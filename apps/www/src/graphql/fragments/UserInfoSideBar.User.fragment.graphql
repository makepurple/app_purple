fragment UserInfoSideBarUser on User {
	desiredSkills(first: 100) {
		pageInfo {
			...PageInfoFragment
		}
		edges {
			cursor
			node {
				id
			}
		}
		nodes {
			id
			name
		}
	}
	followers(first: 0) {
		totalCount
	}
	following(first: 0) {
		totalCount
	}
	friends(first: 0) {
		totalCount
	}
	github {
		id
		bio
		company
		name
		topLanguages {
			...TopLanguages
		}
		twitterUsername
		url
		websiteUrl
	}
	id
	name
	skills(first: 100) {
		pageInfo {
			...PageInfoFragment
		}
		edges {
			cursor
			node {
				id
			}
		}
		nodes {
			id
			name
			owner
		}
	}
	viewerCanFriend
	viewerFollowing
	viewerIsFriend
	...UserAvatarUser
}
