import tw, { styled } from "twin.macro";

export const Typography = styled.span`
	${tw`font-sans`}
	font-size: 1rem;
	font-weight: 500;
	line-height: 1.2em;
	color: inherit;
	margin: 0;
	padding: 0;
	border: 0;
	border-radius: 0;
	box-shadow: none;
	background: transparent;
`;
